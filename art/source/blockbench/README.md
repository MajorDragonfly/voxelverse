# Blockbench Sources

Primary workspace for Voxelverse voxel assets. Keep `.bbmodel` files here and export GLB copies into the matching runtime pack under `res://assets/packs/`.

First production target: `environment/trees/` for `temperate_forest_v1`.
