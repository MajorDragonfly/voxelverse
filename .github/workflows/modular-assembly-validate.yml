name: Modular assembly validation

on:
  push:
    branches: [main, "agent/**"]
  pull_request:

# Keep branch checks and PR merge checks independent; replace only older runs.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  assembly:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-godot-editor
        id: engine
      - name: Import and run real runtime acceptance checks
        run: >-
          python3 tools/validate_godot.py
          --godot "${{ steps.engine.outputs.godot }}"
          --output /tmp/validation-assembly --tests blueprint_contract_test modular_assembly_framework_test asset_pipeline_test --skip-main
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: voxelverse-assembly-${{ github.event_name }}
          path: /tmp/validation-assembly
          retention-days: 7
