# Wiederherstellung und Sichtprüfung

Die erste Flossen-Ausgabe (`fins-export.log`) enthielt einen GDScript-Parsefehler: Beim Wiederherstellen war die neue Default-Platzierung zusätzlich in `duplicate_part` eingefügt worden. Diese überzählige Stelle wurde vor dem Quellcommit entfernt. `fins-export-fixed.log` und der abschließende Vertragslauf sind erfolgreich.

Die anschließende Sichtprüfung korrigierte einen Farbstreifen am breiten Elefantenohr; der Bereich um den Ansatz wird nun radial ausgespart. Die finale Ohren-Baseline und die Ohren-Ansichten wurden anschließend neu erzeugt. Die eingefrorenen alten Modelle blieben unverändert.
